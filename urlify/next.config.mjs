/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['github.com', 'raw.githubusercontent.com'],  // Add GitHub to the allowed domains
    },
  };
  
  export default nextConfig;
  